// Shared layout tokens for the marketing header.
// h-16 (header height) and top-16 (mobile dropdown offset) must stay in sync.
// Update both constants together if the header height ever changes.
export const HEADER_HEIGHT = "h-16";
export const HEADER_OFFSET = "top-16";
